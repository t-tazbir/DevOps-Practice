git log -S "8080" -p
-S "8080": Find commits where the text "PORT" was added or deleted.
-p: Show the full patch (the line-by-line code diff) for those commits so you can see who changed it and what the code looked like before/after.
